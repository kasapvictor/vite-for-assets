// @ts-ignore
// eslint-disable-next-line no-console
console.log('Page #1!');

// @ts-ignore
const init2 = () => {
  return [1, 2, 3].map((n) => n + 1);
};

init2();
// eslint-disable-next-line no-console
console.log([1, 2, 3].map((n) => n + 1));
